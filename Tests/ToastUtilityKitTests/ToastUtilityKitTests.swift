import XCTest
@testable import ToastUtilityKit

final class ToastUtilityKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(ToastUtilityKit().text, "Hello, World!")
        
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
